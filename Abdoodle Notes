You know what this 
_____________________________________________________________________________________________________________________________________________________________________
SYNTAX:
CAPITALIZATION MATTERS| CAPITALIZATION MATTERS|CAPITALIZATION MATTERS|CAPITALIZATION MATTERS|CAPITALIZATION MATTERS|CAPITALIZATION MATTERS|CAPITALIZATION MATTERS|
Use syntax "+=" to add onto a variable: Example_1 += Example_2
Use hashtag to add comment: #Chicken is superior
print "___" to say something,for varia` es print [insert variable name]
to turn string into arthmitic: int()
to join while using print: print [example] + [example]
Float: 7.
Integer: 7
String:"7"
Numbers will round when dividing unless you use float(example)
use """ to combine multiple strings:
address_string = """136 Whowho Rd
Apt 7
Whosville, WZ 44494"""
use \ when using apostrophies
alwasy count from 0\
example of indexs c = "cats"[0]
to print variables use print str() (this turns arithmetic into string)
Len() gets you the character length of what you type
lower() lower cases all capitalization ex:"Ryan".lower()
upper() Upper case all characters ex: "ryan" .upper()
how to use % in strings:print "Hello %s" % (name)(if you want to use % for a variable use %02d instead of %s)
THE ONLY CASE YOU CAN USE , IS IN THE EXAMPLE ABOVE EX: (name, chicken)

User input:
input() is how to set a user input set to a variable EX:
likes_snakes = input("Do you like snakes? ")
-------

Boolean:
True and False must capitalized(yes, I know i contradict myself dont worry about and I dont care if it was easier to capitalize True and False)
set_to_true = True
set_to_false = False
True examples:
1==1
2!=1
not False == True
not 7 < 0       # True
False examples:
3==5
"7"==7
3!=3
not True == False
not 1 + 1 == 2  # False
--------------
how to use 
and,or,use Ex:(1 + 1 == 2) and(2+2==4)

If statement:
Example: 
if 2 == 4 - 2: 
  print("apple")
if age <= 13:
  print("your not old enough")
 ------------
use ==,<=,>=,<,>
make sure to indent when using if statements and use this :

Else statements:
if weekday:
  print("wake up at 6:30")
elif sunday:
  print("wake up at 9:00")
else:
  print("sleep in")
-----------

Random number generator:
To bring this function into use you must use: import random
After this use this code: Example_number_generator = random.randint(Range minimum, Range Maximum)
---

Lists:
You can use integers and strings in a list
Example: heights = [61, 70, 67, 64,"lists"]
append always you to add thing to your list, ex:
append_example.append('list')
To use strings or variables in list use (list name)[1] Ex:
print(example_list[2])
-------
to remove from list use .remove() Ex: example_list.remove("chicken")
Two-dimensional lists examples:
tic_tac_toe = [
            [["X"],["O"],["X"]], 
            [["O"],["X"],["O"]], 
            [["O"],["O"],["X"]]
]

Python counting starts at 0
To modify a list use Ex: Example[2] = "chicken"
How to access 2d lists:
example_list = [[1,2],[3,4]
---
Accessing_2 = example_list [0] [1]
---
To modify 2d list: class_name_hobbies[0][1] = "Meditation"
---
how to use insert:  store_line.insert(2, "Vikor")#the output will be below
.['Karla', 'Maxium', 'Vikor', 'Martim', 'Isabella']
----
how to use pop: removed_element = cs_topics.pop()
-----
how to use range: my_range = range(2,10)
this will create a range of numbers without having to type it all in one
to skip numbers use: my_range = range(2,10,2), the last number is the number it will skip by
---
use len() to find the length of your list
---
Slicing lists: example_list[start:end]
WHEN SPLICING LISTS MAKE SURE TO USE :
---
Use .count()to find how much of something there is
---
.sort() 
(Yes we are still going on about shitty ass lists that make you want to kill yourself)

Math import:
Use "import math" to import math functions 
